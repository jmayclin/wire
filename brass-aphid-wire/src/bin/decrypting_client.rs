const DOMAIN: &str = "amazon.com";
const PORT: u16 = 443;

fn main() {
    // create s2n-tls config

    // disable cert validation because we don't actually care if an endpoint is good

    println!("hello world");
}
